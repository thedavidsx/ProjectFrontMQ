import React               from 'react'
import {withStyles, Theme} from '@material-ui/core/styles'

export interface RutProps {
}

export interface RutState {
}

class Rut extends React.Component<RutProps, RutState> {
  render() {
    return (
      <div>
        
      </div>
    );
  }
}

const styles = (theme: Theme) => ({  
});
export default withStyles(styles) (Rut)