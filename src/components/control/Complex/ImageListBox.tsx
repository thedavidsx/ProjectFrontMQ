import React               from 'react'
import {withStyles, Theme} from '@material-ui/core/styles'

export interface ImageListBoxProps {
}

export interface ImageListBoxState {
}

class ImageListBox extends React.Component<ImageListBoxProps, ImageListBoxState> {
 render() {
    return (
      <div>
        
      </div>
    );
  }
}

const styles = (theme: Theme) => ({  
});
export default withStyles(styles) (ImageListBox)